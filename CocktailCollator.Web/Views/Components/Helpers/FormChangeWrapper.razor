@using CocktailCollator.Web.Common.Interfaces

@typeparam TInputPort where TInputPort : class

@this.ChildContent

@code {
    [EditorRequired, Parameter]
    public RenderFragment ChildContent { get; set; }

    [EditorRequired, Parameter]
    public IFormModel<TInputPort> FormModel { get; set; }

    protected override void OnInitialized()
        => this.FormModel.OnChange = this.StateHasChanged;
}
